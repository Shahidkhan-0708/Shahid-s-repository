#include <bits/stdc++.h>
using namespace std;
void insertion_sort(int arr[],int n){
    for(int i=0;i<n;i++){
        int j=i;
        while(arr[j]<arr[j-1]&&j>0)
        {
            swap(arr[j],arr[j-1]);
            j--;
        }
        
     }
    
    
    
    }
int main() {
int arr[]={22,222,221,33,2,21};
int n=sizeof(arr)/sizeof(arr[0]);
   insertion_sort(arr,n);
   for(int i=0;i<n;i++){
       cout<<arr[i]<<" ";
       
       
   }
    }
